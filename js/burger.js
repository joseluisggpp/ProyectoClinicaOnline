let showBurger = document.getElementById("showBurger")
      let closeBurger = document.getElementById("closeBurger")
      let itemsBurger = document.getElementById("items-burger")

      showBurger.addEventListener("click", () => {
        //TODO pillar el modal y mostrarlo add class
        itemsBurger.classList.toggle("showBurger")
      })

      closeBurger.addEventListener("click", () => {
        //TODO pillar el modal y mostrarlo add class
        itemsBurger.classList.toggle("showBurger")
      })